[
  {
    "comment": "Add your test case here for debugging and run go test -run=One.",
    "query": "select bing, sum(amount), count(*) from (select user.foo * user_extra.bar - music.boing as amount, user.bing from user, user_extra, music where user.id = user_extra.id and user_extra.bla = music.poop) as foo group by bing order by sum(amount)",
    "plan": {

    }
  }
]