# Add your test case here for debugging and run go test -run=One.
"select 1 from user join unsharded as m1 join unsharded as m2 where user.id = 42"
{
  "QueryType": "SELECT",
  "Original": "select user.col from user join user_extra on user.id = user_extra.user_id where user.col = 42 and user_extra.col = 43 and user.col2 = user_extra.col2",
  "Instructions": {
    "OperatorType": "Route",
    "Variant": "SelectScatter",
    "Keyspace": {
      "Name": "user",
      "Sharded": true
    },
    "FieldQuery": "select user.col from user join user_extra on user.id = user_extra.user_id where 1 != 1",
    "Query": "select user.col from user join user_extra on user.id = user_extra.user_id where user.col = 42 and user_extra.col = 43 and user.col2 = user_extra.col2",
    "Table": "user"
  }
}
