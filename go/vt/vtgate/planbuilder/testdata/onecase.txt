# Add your test case here for debugging and run go test -run=One.
"select user_id from authoritative order by user_id"
{
  "QueryType": "SELECT",
  "Original": "select user_id from authoritative order by user_id",
  "Instructions": {
    "OperatorType": "Route",
    "Variant": "SelectScatter",
    "Keyspace": {
      "Name": "user",
      "Sharded": true
    },
    "FieldQuery": "select user_id, weight_string(user_id) from authoritative where 1 != 1",
    "OrderBy": "0 ASC",
    "Query": "select user_id, weight_string(user_id) from authoritative order by user_id asc",
    "Table": "authoritative"
  }
}
{

}
